# 028 — Source Manifest Output Overrides

NONE OF THIS IS CANONICAL UNTIL VERIFIED DIRECTLY BY SAB 
------------------------------------------------ Agent Section START

Date: 2026-07-02
Worktree: `/home/sab/myapi`
Branch: `main`

## Empirical Reality (2-3 sentences max, anything more must be critically justifiable)

Added the smallest downstream-consumable path for active corpus manifests: `scripts/source_manifest.py` can now write JSON to `--output` and replace default Mac roots with one or more `--source-root` overrides in corpus-manifest mode. MyMCP transport remains untouched; this is still MyAPI-owned corpus manifest plumbing.

### Scope touched (One file per line, +/- for only what was changed)

+ `scripts/source_manifest.py`
+ `tests/test_source_manifest.py`
+ `.handoffs/028-source-manifest-output-overrides.md`

### Current Git state (2-3 sentences max, anything more must be critically justifiable)

Expected final state after commit/push: `main` synced with `origin/main`. No generated manifest files are intentionally left in the worktree.

### Artifacts (Filepath - Description, 1 line max per artifact)

`scripts/source_manifest.py` - Adds `--output`, repeated `--source-root`, `sources_from_roots()`, and `emit_json()`.
`tests/test_source_manifest.py` - Covers local source-root overrides, output-file writing, and CLI active-manifest generation.

### Resume point (2-3 sentences max, anything more must be critically justifiable)

Next slice: choose a stable default output location or calling script for daily active manifests, then let MyAPI reader/brief assembly consume that manifest. Keep MyMCP as the two-tool doorway until MyAPI has a bounded brief/cache surface to call.
------------------------------------------------ Agent Section END

------------------------ Do NOT edit this file past this point

## Narrative / Trajectory (SAB ONLY)

### Intent going into/at start of session

### Interpretation of how the session went

### Friction experienced or anticipated

### What's Next (Momentum or Lack Thereof)
