# 030 — Decision Graph Implementation Plan

------------------------------------------------ Agent Section START

Date: 2026-08-10
Worktree: `/Users/sab-mini/repos/MyAPI-rebuild`
Branch: `codex/myapi-decision-plan`

## Empirical Reality (2-3 sentences max, anything more must be critically justifiable)

The corpus-first scaffold has been replaced by an eight-node Decision Graph plan that uses MyAPI-rebuild as its own first subject corpus. The domain glossary fixes `DecisionCandidate`, accepted `Decision`, evidence, acceptance, divergence, and `ContextBrief` boundaries; the existing Graphify query currently surfaces only the top-level `MyAPI-Rebuild` node.

### Scope touched (One file per line, +/- for only what was changed)

`+ CONTEXT.md`
`+ .handoffs/030-decision-graph-implementation-plan.md`
`± AGENTS.md`
`± IMPLEMENTATION-PLAN.md`
`± .gitignore`

### Constrained areas touched (none / list + justification)

`.gitignore` — ignores Graphify's machine-local interpreter and root guards created during the required existing-graph query.

### Current Git state (2-3 sentences max, anything more must be critically justifiable)

This checkpoint belongs entirely to `codex/myapi-decision-plan` and contains documentation plus machine-local Graphify ignore rules. The intended checkpoint leaves the branch clean after commit and push.

### Artifacts (Filepath - Description, 1 line max per artifact)

`IMPLEMENTATION-PLAN.md` - Executable M1–M8 Decision Graph plan and acceptance gates.
`CONTEXT.md` - Canonical MyAPI Decision Graph language.
`AGENTS.md` - Cold-start routing and work order aligned to the accepted Decision direction.

### Resume point (2-3 sentences max, anything more must be critically justifiable)

Begin M1 by creating `evals/decision-mvp-v0.yaml` from MyAPI questions in `evals/eval-bank-v0.md` and verified MyAPI history. Freeze five verified cases first, validate the case schema, then expand the same artifact to exactly twenty before M2 begins.

------------------------------------------------ Agent Section END

------------------------ Do NOT edit this file past this point

## Narrative / Trajectory (SAB ONLY)

### Intent going into/at start of session

### Interpretation of how the session went

### Friction experienced or anticipated

### What's Next (Momentum or Lack Thereof)
