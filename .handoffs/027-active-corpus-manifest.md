# 027 — Active Corpus Manifest

------------------------------------------------ Agent Section START

Date: 2026-07-02
Worktree: `/home/sab/myapi`
Branch: `main`

## Empirical Reality (2-3 sentences max, anything more must be critically justifiable)

Implemented the first MyAPI-owned source normalization slice: per-file manifest rows now stamp source metadata plus `hot`/`durable`/`cold`, and `--active-only` emits the active hot+durable bundle manifest. MyMCP remains untouched as a two-tool doorway; this change stays in MyAPI corpus policy/manifest code.

### Scope touched (One file per line, +/- for only what was changed)

+ `context_refinery/normalization_schema.py`
+ `scripts/source_manifest.py`
+ `tests/test_normalization_schema.py`
+ `tests/test_source_manifest.py`
+ `.handoffs/027-active-corpus-manifest.md`

### Current Git state (2-3 sentences max, anything more must be critically justifiable)

Expected final state after commit/push: `main` synced with `origin/main`. No generated artifacts are intentionally left untracked.

### Artifacts (Filepath - Description, 1 line max per artifact)

`scripts/source_manifest.py` - Adds `--corpus-manifest-json`, `--active-only`, `--hot-days`, tier decisions, and active bundle manifest rows.
`context_refinery/normalization_schema.py` - Adds optional `corpus_tier`/`tier_reason` fields and `project-documents/` source typing.
`tests/test_source_manifest.py` - Covers hot/durable/cold classification and active-only bundle filtering.

### Resume point (2-3 sentences max, anything more must be critically justifiable)

Next slice: let the manifest accept repo/local source overrides or write the active manifest to a stable output path for downstream indexing. After that, wire MyAPI brief assembly/cache code to consume active hot+durable manifests before touching MyMCP transport.
------------------------------------------------ Agent Section END

------------------------ Do NOT edit this file past this point

## Narrative / Trajectory (SAB ONLY)

### Intent going into/at start of session

### Interpretation of how the session went

### Friction experienced or anticipated

### What's Next (Momentum or Lack Thereof)
