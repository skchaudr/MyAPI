# MyAPI — QUERIES (mini pass-1)

Agent-shaped questions this corpus-hot should answer from files.

## Ownership

1. Who owns context engine vs doorway — MyAPI vs MyMCP?
2. Which repo path is canonical for rebuild direction vs implementation phases?
3. Who may write graph/corpus truth vs who only reads/fixtures (golden briefs)?

## Now-state

4. What is the current phase — planning/docs only, or implementation code live?
5. What is the live `main` tip and what did the last commit change?
6. What is the active corpus policy (15–30 day hot window vs Corpus v1.0 cold)?
7. What does `myapi-db-plan.md` supersede (`prove-first-durable-handoff`)?

## Evidence

8. What evidence paths prove the two MCP tools and their fixture-backed answers?
9. Which handoff documents the GDDP first durable handoff node for MyAPI?
10. What graphify/god-node snapshot was frozen in PROJECT-BRIEF (date + counts)?

## Connect A→B

11. How does L0 corpus → L1 graphify → L2 packs → L3 MCP chain in IMPLEMENTATION-PLAN?
12. How does MyAPI-rebuild sit with `.pi` and GDDP as the portfolio triptych?
13. How do golden briefs connect cold-agent evals to `get_project_context` / `get_person_context`?
14. How does Corpus v1.0 failure mode (ChatGPT volume, unsourced CLI sessions) force handoff-first redesign?

## Steal shapes (graphify-class)

15. "What are the top god nodes for MyAPI-rebuild and which community is MCP surface?"
16. "Path from REBUILD-CONTEXT-ANCHOR to get_person_context naming lock."
