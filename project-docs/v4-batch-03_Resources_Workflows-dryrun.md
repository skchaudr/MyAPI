# Vault Schema V4 Normalization Dry Run

Vault root: `/Users/saboor/Obsidian/SoloDeveloper`

## Summary

- Total files scanned: 168
- High confidence: 3
- Medium confidence: 165
- Needs review: 0

## Review Queue

### 03 Resources/Workflows/Ghostty + Obsidian CLI Workflow.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: unprefixed tag `workflow`; unprefixed tag `terminal`; unprefixed tag `productivity`; unprefixed tag `obsidian`; unprefixed tag `ghostty`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/00 - Skill Tree System (README).md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: unprefixed tag `system`; unprefixed tag `skilltree`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/10 - Branch Nodes (All Branches).md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: unprefixed tag `skilltree`; unprefixed tag `branches`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/99 - Skill Tree Dashboard.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: unprefixed tag `skilltree`; unprefixed tag `dashboard`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/10 Motions That Feel Natural.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/10 Pipe Chains That Solve Problems.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/10 Sparring Sessions Logged.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Antigravity Deep Dive Session.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Architecture Discussion.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Ask Why Five Times.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Autocommands Basics.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/BDR Ad Campaign Ready.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/BDR Admin Dashboard MVP.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/BDR Analytics Setup.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/BDR Architecture Doc.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/BDR Booking Calendar Integration.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/BDR Booking Calendar Working.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/BDR Booking Form Submission.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/BDR Codebase Orientation.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/BDR Deployed to Production.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/BDR Email Notifications.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/BDR Local Dev Environment.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/BDR Local SEO.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/BDR Mobile Responsive.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/BDR Payment Integration.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/BDR Performance Audit.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/BDR Performance Fixes.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/BDR Photo Gallery Polished.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/BDR Pricing Logic.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/BDR Project Architecture Doc.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/BDR README Accurate.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/BDR Reviews Integration.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/BDR SEO Basics.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/BDR SEO Foundation.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/BDR Supabase Schema Solid.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Break the AI Solution.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Burn 50 Credits Strategically.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Claude Auditor Workflow.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Colorscheme Thats Yours.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Cron Job on Pi.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Custom Function in Lua.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/DAP Debug Adapter Setup.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/DSA Problem Category Deep Dive.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Daily Driver Note Template.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Debug a Plugin Issue.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Explain It Back.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/File Navigation with Telescope.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/First AI Sparring Session.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/First Recruiter Outreach.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/First Shell Script That Saves Time.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/First Technical Interview.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Git Integration.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Git from CLI Only.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Gym Hour Automation.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Install and Configure 3 Plugins.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Jules Async Delegation.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Jules Audit Workflow.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Jump List Navigation.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/LSP Setup.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/LeetCode Pattern Recognition.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Macros for Repetitive Edits.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Marks Local and Global.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Mock Interview with AI.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Model Comparison Sparring.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Model Strengths Map.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Monitoring and Logging on Pi.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Multi-Agent Pipeline.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/NeoVim tmux Seamless Navigation.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Network Debugging Toolkit.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Options You Actually Set.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Pair with Antigravity on Config.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Pi Cluster Distributed Task.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Pi Runs Real Service.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Pi Serves HTTP Locally.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Pi in SSH Config.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Portfolio Site Live.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Process Management.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Profile and Optimize Startup.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Project Command Center.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Prompt Engineering Experiment.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Question the Big-O.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Quickfix List Workflow.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Real Bug AI Fix Your Verification.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Refactor with AI Critique.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Registers Beyond Basics.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Remote tmux from iPad.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Resume 2.0 with Real Projects.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Rewrite Without Looking.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/SSH Key Auth to Pi.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Scripted Deployment to Pi.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Search Until Muscle Memory.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Second Pi Online.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Shell Script with Args and Conditionals.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Snippets Workflow.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/SocialXP Authentication.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/SocialXP Core Feature Prototype.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/SocialXP Database Schema.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/SocialXP Deployment.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/SocialXP Dissertation Context.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/SocialXP Documentation.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/SocialXP Mobile Responsive.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/SocialXP Privacy Ethics.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/SocialXP Repo Setup.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/SocialXP Research Landscape.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/SocialXP Second Feature.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/SocialXP Tech Stack Decision.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/SocialXP UI Components.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/SocialXP User Flow Wireframes.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/SocialXP User Testing.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/SocialXP Vision Alignment.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Solve It First Then Compare.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Solve Something with awk.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Solve Something with sed.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Spar on a Real Bug.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Splits and Buffers Workflow.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Spot the Code Smell.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Spot the Hallucination.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Statusline Configured.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Substitute Mastery.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Survive a Full Day in NeoVim.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/System Design Sparring.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/System Design with AI Debate.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Systemd Service.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Teach AI Sparring to Someone.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Teach AI Your Codebase.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Teach Someone One NeoVim Trick.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Terminal Prompt Customized.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Text Objects Deep Dive.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/The Dot Command.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/The Socratic Prompt.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/The g Commands.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Time Complexity Analysis.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Trace the Logic Line by Line.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Trace the Logic Step by Step.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Treesitter for Syntax.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Two Pis Talking.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Understand Your Config Structure.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Undo Tree.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Unlock - AI Fluent.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; unprefixed tag `unlock`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Unlock - AI Multiplied.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; unprefixed tag `unlock`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Unlock - AI Strategist.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; unprefixed tag `unlock`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Unlock - AI Whisperer.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; unprefixed tag `unlock`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Unlock - BDR Business Ready.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; unprefixed tag `unlock`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Unlock - BDR MVP Live.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; unprefixed tag `unlock`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Unlock - Editor Wizard.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; unprefixed tag `unlock`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Unlock - NeoVim as IDE.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; unprefixed tag `unlock`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Unlock - Remote Ops.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; unprefixed tag `unlock`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Unlock - SocialXP Dissertation Ready.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; unprefixed tag `unlock`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Unlock - SocialXP Foundation Solid.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; unprefixed tag `unlock`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Unlock - SocialXP Prototype Complete.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; unprefixed tag `unlock`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Unlock - Sparring Partner.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; unprefixed tag `unlock`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Unlock - Terminal Wizard.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; unprefixed tag `unlock`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Unlock - Your Config Your Editor.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; unprefixed tag `unlock`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Unlock - Your Toolbox.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; unprefixed tag `unlock`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Visual Mode Variants.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Weekly Review Dashboard.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Write Your Own Plugin.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Write the Test Cases First.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Your First Custom Keybind.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/jq Script Pipeline.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/jq Transform on AI Exports.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/ripgrep fd Power Moves.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/tmux Session Workflow.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/xargs for Batch Operations.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: status exists on non-project/event type; unprefixed tag `skilltree`; unprefixed tag `quest`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Templates/TEMPLATE - Quest.md

- Confidence: `medium`
- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
- Review needed: unprefixed tag `template`; unprefixed tag `skilltree`; concepts need owner assignment
- Suggested:
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Templates
migration_status: v4-dry-run
```

## High-Confidence Sample

### 03 Resources/Workflows/2026 Workflows based around Obsidian and build from there.md

- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/SkillTree-test.md

- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree
migration_status: v4-dry-run
```

### 03 Resources/Workflows/SkillTree/Skills/Dotfiles v1.md

- Destination: `resources/`
- Reasons: top-level folder `03 Resources` maps to `resources/`; tag inferred from resource folder `Workflows`; area inferred from resource folder `Workflows`
```yaml
type: resource
area: '[[My_DevInfra]]'
tags:
- topic/workflow
source: imported
folder_origin: 03 Resources/Workflows/SkillTree/Skills
migration_status: v4-dry-run
```
