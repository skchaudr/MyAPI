# pi-needle-gemma / QUERIES — merged v1

Date: 2026-07-29
Mini = repo/prod bias. Air = vault/operator bias. Both kept as evidence.

---

## From mini

# Pi / Needle / Gemma — QUERIES (mini pass-1)

## Ownership

1. What does Pi own vs Needle vs mac-needle training package?
2. Who decides local-verb vs frontier delegate — Needle router or frontier model?
3. Where do model provider settings live vs where weights/checkpoints live?

## Now-state

4. What is Needle’s preferred host for neural routing (Mac vs VM) and why?
5. What are the five verbs in tools.json?
6. Is Needle observe-only or active routing until eval gates clear?
7. What are the two active Pi portfolio threads (extensions audit + Needle)?

## Evidence

8. Which handoffs document Mac routing decision and cost evidence?
9. Which paths prove Gemma CLI / build scripts without loading weights?
10. What does PROJECT-BRIEF claim for Needle v2 ship gate metrics?

## Connect A→B

11. How does a packet run through harness produce verified artifacts vs confabulated claims?
12. How does Pi verify peer agents (Hermes/Codex/Claude) in the portfolio triptych with MyAPI + GDDP?
13. How does `pi-route` / `NEEDLE_URL` connect orchestrator to serve.py daemon?

## Graphify-class

14. What layers appear in Pi’s ground-state graph (harness, extensions, TUI, needle)?
15. Path from tools.json registry → router.route → shadow/eval before promotion.


---

## From air

# Pi / Needle / Gemma — QUERIES (air)

## Ownership
1. What does **Needle** own (route/assemble/serve) vs **Pi agent** (`~/.pi/agent`) vs **harness**?
2. Who owns the **needle-gemma-v1 bundle** schemas vs Camber upload approval?
3. Who may launch a Camber job/app/engine (Sab gate per ledger)?

## Now-state
4. Does air have a complete local `needle-gemma-v1` bundle with manifest + schemas?
5. What model config files exist (`models.json`, ollama functiongemma) without loading weights?
6. Is Needle serve configured via `com.needle.serve.plist` on this host?
7. What does the execution ledger say status is (Part 1 local vs upload vs train)?

## Evidence
8. Where is the Camber launch approval contract written (duration, budget, shutdown)?
9. Where is harness choice / NEEDLE handoff documented?
10. What vault notes capture Needle×Gemma×Pi postmortem and execution plan?

## Connect
11. How does Needle routing inventory (`routing-inventory/staged/*_report.json`) relate to multi-host (sab-air, pi-big, pi-small)?
12. How does Gemma row schema connect to eval assembler jsonl without treating training dumps as corpus?
13. How should MyAPI golden brief `get_project_context_pi_needle` (on MyAPI main) attach to these paths?
14. What must never be swallowed by Khoj/corpus ingest from `~/.pi` (auth, weights, quarantine, full session dumps)?

