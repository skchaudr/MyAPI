# myapi / QUERIES — merged v1

Date: 2026-07-29
Mini = repo/prod bias. Air = vault/operator bias. Both kept as evidence.

---

## From mini

# MyAPI — QUERIES (mini pass-1)

Agent-shaped questions this corpus-hot should answer from files.

## Ownership

1. Who owns context engine vs doorway — MyAPI vs MyMCP?
2. Which repo path is canonical for rebuild direction vs implementation phases?
3. Who may write graph/corpus truth vs who only reads/fixtures (golden briefs)?

## Now-state

4. What is the current phase — planning/docs only, or implementation code live?
5. What is the live `main` tip and what did the last commit change?
6. What is the active corpus policy (15–30 day hot window vs Corpus v1.0 cold)?
7. What does `myapi-db-plan.md` supersede (`prove-first-durable-handoff`)?

## Evidence

8. What evidence paths prove the two MCP tools and their fixture-backed answers?
9. Which handoff documents the GDDP first durable handoff node for MyAPI?
10. What graphify/god-node snapshot was frozen in PROJECT-BRIEF (date + counts)?

## Connect A→B

11. How does L0 corpus → L1 graphify → L2 packs → L3 MCP chain in IMPLEMENTATION-PLAN?
12. How does MyAPI-rebuild sit with `.pi` and GDDP as the portfolio triptych?
13. How do golden briefs connect cold-agent evals to `get_project_context` / `get_person_context`?
14. How does Corpus v1.0 failure mode (ChatGPT volume, unsourced CLI sessions) force handoff-first redesign?

## Steal shapes (graphify-class)

15. "What are the top god nodes for MyAPI-rebuild and which community is MCP surface?"
16. "Path from REBUILD-CONTEXT-ANCHOR to get_person_context naming lock."


---

## From air

# MyAPI — QUERIES (air)

Agent-shaped questions this corpus should answer without an empty graphify call.

## Ownership
1. Who owns the **durable handoff** format vs the **MCP brief** response shape?
2. Who owns **corpus admission** (vault normalize) vs **retrieval** (Khoj/VM vs local reader)?
3. Is **MyMCP** a separate product surface or a thin paid doorway into MyAPI? (per main PROJECT-BRIEF)
4. Who advances the **GDDP node** `prove-first-durable-handoff` — human acceptance only, or runtime?

## Now-state
5. What branch/tip is air’s MyAPI clone on, and what is missing vs `main`?
6. What is the current north star: corpus v1 vault substrate, or two MCP tools over v2 durable handoffs?
7. Which golden briefs exist and what intent do they encode (`get_project_context` / `get_person_context`)?
8. Is Vertex/ADC still an open workstream or parked (VM done)?

## Evidence
9. Where is the first **GDDP-linked** MyAPI handoff node defined, and what does acceptance require?
10. What evidence shows v0 RAG failure mode (ChatGPT volume dominance, CLI sessions ingested but not surfaced)?
11. Point to operator notes that redefine “normalization” as vault batch + compare structured vault vs API retrieval.

## Connect (A→B)
12. How does **Corpus v1.0** bucket taxonomy map to rebuild layers L0–L4 / context_refinery?
13. How does MyAPI handoff `029` / gddp node connect to **gddp-config** node schema?
14. How do air vault notes (10-task checklist, “what normalization is becoming”) connect to main IMPLEMENTATION-PLAN phases?
15. Steal-from-graphify shape: given a symbol or file name from MyAPI `graphify-out`, what **project narrative** brief should attach (not just AST label)?

